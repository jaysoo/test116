# pkg-7

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pkg-7` to build the library.

## Running unit tests

Run `nx test pkg-7` to execute the unit tests via [Jest](https://jestjs.io).

# pkg-23

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pkg-23` to build the library.

## Running unit tests

Run `nx test pkg-23` to execute the unit tests via [Jest](https://jestjs.io).

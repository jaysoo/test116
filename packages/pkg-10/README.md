# pkg-10

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pkg-10` to build the library.

## Running unit tests

Run `nx test pkg-10` to execute the unit tests via [Jest](https://jestjs.io).

# pkg-16

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pkg-16` to build the library.

## Running unit tests

Run `nx test pkg-16` to execute the unit tests via [Jest](https://jestjs.io).

# pkg-22

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pkg-22` to build the library.

## Running unit tests

Run `nx test pkg-22` to execute the unit tests via [Jest](https://jestjs.io).
